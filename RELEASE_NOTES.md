# v1.0.0

Initial public release.

- Forces the first Circle to Act translation target to Korean
- Leaves explicitly selected target languages unchanged
- Starts automatically after Windows sign-in
- Survives reboot
- Includes clean uninstall
- Preserves and restores pre-existing app-specific WebView2 arguments
- Uses transparent source files instead of an embedded Base64 installer
- Adds diagnostic logging
